{
  "name": "youtube-radar-backend",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --coverage --coverage.reporter=text --coverage.reporter=json-summary"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1106.0",
    "@aws-sdk/lib-dynamodb": "^3.1106.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.9",
    "osls": "^3.55.0",
    "vitest": "^2.1.9"
  }
}
